{% comment %}
  Back to Top Button (App Embed version with Auto-Injection)
{% endcomment %}

{{ 'storefront-suite.css' | asset_url | stylesheet_tag }}

<button type="button" class="back-to-top-floating-btn" id="btt-btn-{{ block.id }}" style="display: none;" onclick="scrollToTopSmooth()">
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" class="btt-icon">
    <polyline points="18 15 12 9 6 15"></polyline>
  </svg>
</button>

<script>
  function scrollToTopSmooth() {
    window.scrollTo({
      top: 0,
      behavior: 'smooth'
    });
  }

  (function() {
    document.addEventListener('DOMContentLoaded', function() {
      initBackToTop();
    });

    function initBackToTop() {
      const btn = document.getElementById('btt-btn-{{ block.id }}');
      if (!btn) return;

      const threshold = parseInt('{{ block.settings.scroll_threshold | default: 300 }}', 10);

      btn.style.display = 'flex';
      
      // Set background color inline since it is a customizable color setting
      btn.style.backgroundColor = '{{ block.settings.bg_color | default: "#10b981" }}';
      btn.style.color = '{{ block.settings.icon_color | default: "#ffffff" }}';

      window.addEventListener('scroll', function() {
        if (window.scrollY >= threshold) {
          btn.classList.add('is-visible');
        } else {
          btn.classList.remove('is-visible');
        }
      });
    }
  })();
</script>

{% schema %}
{
  "name": "Back to Top Button",
  "target": "body",
  "settings": [
    {
      "type": "number",
      "id": "scroll_threshold",
      "label": "Scroll Depth Threshold (px)",
      "default": 300
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Button Background Color",
      "default": "#10b981"
    },
    {
      "type": "color",
      "id": "icon_color",
      "label": "Arrow Icon Color",
      "default": "#ffffff"
    }
  ]
}
{% endschema %}
